const { createConfig } = require('@openedx/frontend-build');

module.exports = createConfig('webpack-dev', {
    // devServer: {
    //     allowedHosts: 'all',
    //     server: 'https',
    // },
});
